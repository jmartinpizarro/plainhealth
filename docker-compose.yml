services:
  backend:
    build:
      context: .
      dockerfile: src/api/Dockerfile
    env_file:
      - .env
    gpus: all
    ports: 
      - "5000:5000"

  frontend:
    build: ./src/frontend
    ports:
      - "5173:5173"
